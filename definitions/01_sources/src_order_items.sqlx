config {
  type: "view",
  schema: "01_import",
  name: "src_order_items",
  description: "注文明細のソースview"
}

SELECT
    order_id,
    order_item_id,
    product_id,
    seller_id,
    shipping_limit_date,
    price,
    freight_value
FROM ${ref("order_items")}
